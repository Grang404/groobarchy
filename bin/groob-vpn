#!/usr/bin/env bash
if nmcli connection show --active | grep wireguard &>/dev/null || nmcli connection show --active | grep vpn &>/dev/null; then
	echo '{"text":"","class":"connected"}'
else
	echo '{"text":"","class":"disconnected"}'
fi
