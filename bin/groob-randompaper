#!/bin/env bash

WALLPAPER_DIR="$GROOB_DIR/wallpapers"

file=$(find "$WALLPAPER_DIR" -type f | shuf -n 1)

awww img --resize=crop --transition-type=wipe --transition-angle=30 --transition-step=20 --transition-fps=60 "$file"
