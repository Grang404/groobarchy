$font = JetBrains Nerd Font Mono

general {
    hide_cursor = false
}

animations {
    enabled = true
    bezier = linear, 1, 1, 0, 0
    animation = fadeIn, 1, 2, linear
    animation = fadeOut, 1, 2, linear
    animation = inputFieldDots, 1, 2, linear
}

background {
    monitor =
    path = screenshot
    blur_passes = 3
}

input-field {
    monitor =
    font_family = $font
    font_color = rgba(c9c7cdff)

    size = 20%, 5%
    outline_thickness = 3
    inner_color = rgba(0, 0, 0, 0.0)
    outer_color = rgba(757581ff)
    check_color = rgba(b9aedaff)
    fail_color = rgba(d44657ff)
    capslock_color = rgba(d44657ff)

    fade_on_empty = false
    rounding = 5

    placeholder_text = Input password...
    fail_text = $FAIL <b>($ATTEMPTS)</b>

    dots_spacing = 0.3
    position = 0, -20
    halign = center
    valign = center
}

# TIME
label {
    monitor =
    text = $TIME
    font_size = 90
    font_family = $font
    color = rgba(c9c7cdff)
    position = 0, 200
    halign = center
    valign = center
}

# DATE
label {
    monitor =
    text = cmd[update:60000] date +"%A, %d %B %Y"
    font_size = 25
    font_family = $font
    color = rgba(c9c7cdff)
    position = 0, 120
    halign = center
    valign = center
}
